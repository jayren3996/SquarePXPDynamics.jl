# Agent Instructions

Before substantial work, read `memory/README.md` and follow its read order for
project goals, architecture, decisions, current state, and handoff context.

Run `project-memory-curator` only when explicitly requested by the user. After
substantial work, update short-term handoff files only when asked. Record
important scientific, architectural, implementation, and workflow decisions in
`memory/mid_term/decision_log.md`.
